{
  "name": "shortener",
  "version": "1.0.0",
  "description": "URL Shortener Web Application",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "cd client && npm start",
    "server": "cd app && nodemon server",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "author": "Andy Mardesich",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "express": "^4.17.1",
    "morgan": "^1.10.0",
    "nanoid": "^3.1.20",
    "valid-url": "^1.0.9"
  },
  "devDependencies": {
    "concurrently": "^5.3.0"
  }
}
